package net.strangled.maladan

/**
 * MaladaN (R)
 */
class Main {
    static void main(String[] args) {
        if (args[0] == '--help' || args[0] == '-h') {
            Documentation.printInstructions()
        } else if (args[0] == '--atomic-radius' || args[0] == '-ar') {
            
        }
    }
}
